import React from "react";
import { SideMenuProps } from "../../types";

export const SideMenu: React.FC<SideMenuProps> = (props) => {
  return <nav id="side-menu">side navigation bar ........</nav>;
};
